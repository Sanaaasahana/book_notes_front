[build]
  publish = "client"   # Points to your static files folder
  command = ""         # No build step needed

# For single-page apps (like React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200


